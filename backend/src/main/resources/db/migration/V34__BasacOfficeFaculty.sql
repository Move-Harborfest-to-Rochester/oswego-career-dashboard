CREATE TABLE basac_office_faculty
(
    id    BINARY(16)   NOT NULL,
    name  VARCHAR(255) NULL,
    title VARCHAR(255) NULL,
    email VARCHAR(255) NULL,
    CONSTRAINT pk_basacofficefaculty PRIMARY KEY (id)
);